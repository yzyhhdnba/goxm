# Deploy Notes

`deploy/` 用于存放本地依赖、初始化脚本与后续部署相关材料。

## 当前目录

- `mysql/init/`
  MySQL 初始化 SQL。

- `rocketmq/`
  RocketMQ 本地联调用到的配置文件。

## 使用边界

- 当前 `deploy/` 主要服务于本地开发和联调，不代表完整生产部署方案。
- 如果未来补充容器镜像、环境样例或部署脚本，也应优先收敛到这个目录。
- 与运行环境强绑定的个人配置，不要直接写进这里。
